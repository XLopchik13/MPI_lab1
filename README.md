# Лабораторная работа: Параллельное программирование с MPI

Две задачи на параллельное программирование с использованием MPI:
1. Вычисление числа π методом Монте-Карло
2. Умножение матрицы на вектор (3 алгоритма: по строкам, столбцам и блокам)

## 📋 Требования

### 1. MS-MPI (Microsoft MPI)
Скачать и установить:
- [MS-MPI Runtime](https://www.microsoft.com/en-us/download/details.aspx?id=100593) - `msmpisetup.exe`
- [MS-MPI SDK](https://www.microsoft.com/en-us/download/details.aspx?id=100593) - `msmpisdk.msi`

После установки добавьте в PATH (если не добавилось автоматически):
```
C:\Program Files\Microsoft MPI\Bin
```

Проверка установки:
```powershell
mpiexec -help
```

### 2. MinGW (MSYS2)
Уже установлен. Убедитесь, что `gcc` доступен:
```powershell
gcc --version
```

### 3. Python и библиотеки
Установить необходимые библиотеки:
```powershell
pip install numpy matplotlib pandas
```

## 📁 Структура проекта

```
lab1_mpi/
├── task1_pi/              # Задача 1: вычисление π
│   ├── pi_monte_carlo.c
│   └── Makefile
├── task2_matvec/          # Задача 2: матрица × вектор
│   ├── matvec_rows.c      # Разбиение по строкам
│   ├── matvec_cols.c      # Разбиение по столбцам
│   ├── matvec_blocks.c    # Разбиение по блокам
│   └── Makefile
├── python_analysis/       # Скрипты для анализа
│   ├── run_experiments.py
│   ├── analyze_results.py
│   └── plot_graphs.py
├── results/               # Результаты экспериментов
├── requirements.txt
└── README.md
```

## 🚀 Быстрый старт

### 1. Установка зависимостей Python
```powershell
pip install -r requirements.txt
```

### 2. Компиляция программ

**Задача 1:**
```powershell
cd task1_pi
make
```

**Задача 2:**
```powershell
cd task2_matvec
make
```

### 3. Запуск экспериментов

**Вручную (пример):**
```powershell
# Задача 1: π с 4 процессами
mpiexec -n 4 .\task1_pi\pi_monte_carlo.exe 10000000

# Задача 2: матрица×вектор (по строкам) с 8 процессами
mpiexec -n 8 .\task2_matvec\matvec_rows.exe 5000
```

**Автоматически (все эксперименты):**
```powershell
cd python_analysis
python run_experiments.py
```

### 4. Анализ и построение графиков
```powershell
python analyze_results.py
python plot_graphs.py
```

## 📊 Результаты

Скрипты Python построят графики:
- Время выполнения vs количество процессов
- Ускорение (speedup) vs количество процессов
- Эффективность vs количество процессов

Графики сохраняются в `results/`

## 🔧 Параметры экспериментов

**Задача 1 (π):**
- Количество точек: 1,000,000 - 50,000,000
- Процессы: 1, 2, 4, 8

**Задача 2 (матрица × вектор):**
- Размеры матриц: 1000×1000, 3000×3000, 5000×5000
- Процессы: 1, 2, 4, 8
- Алгоритмы: по строкам, по столбцам, по блокам

## 📝 Заметки

- Для корректных измерений времени запускайте на выделенной системе
- Первый запуск может быть медленнее из-за кеширования
- Результаты зависят от характеристик процессора и количества ядер